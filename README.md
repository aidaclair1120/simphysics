# Sim HSC Physics

Browser prototype for the electrical minigame.

## Run

```powershell
npm install
npm run dev
```

Then open the local URL Vite prints.

## Checks

```powershell
npm run build
npm test
```

Current prototype shows an ECS-backed circuit in a debug UI. The solver and interactive renderer are next.
